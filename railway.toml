[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo 'Starting app...' && alembic upgrade head && echo 'Migrations done, starting uvicorn...' && exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
